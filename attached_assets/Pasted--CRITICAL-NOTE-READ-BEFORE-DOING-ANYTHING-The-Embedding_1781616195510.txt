⚠️ CRITICAL NOTE — READ BEFORE DOING ANYTHING:
The Embedding Service is intentionally running on TF-IDF fallback mode.
Do NOT install sentence-transformers. Do NOT install torch. Do NOT download any model files.
Do NOT run any pip install command related to ML/AI libraries.
The TF-IDF fallback is sufficient for all development sessions.
Treat the Embedding Service as FROZEN — do not touch it.

This is Phase 11 — Prompt 8: SEO Layer.
Execute every step completely and in order. Do not skip any step. Do not summarize. Show full output for every step.

═══════════════════════════════════════
PART 1 — SEO LAYER IMPLEMENTATION
═══════════════════════════════════════

STEP 1.1 — Read existing code before writing anything:
Read these files in full before touching anything:
  - artifacts/marketplace/src/main.tsx
  - artifacts/marketplace/src/App.tsx
  - artifacts/marketplace/index.html
  - artifacts/marketplace/src/i18n/ar.json
  - artifacts/marketplace/src/i18n/en.json
  - artifacts/marketplace/src/pages/ (list all files)
  - artifacts/api-server/src/routes/ (list all files)
  - CURRENT_STATE.md
  - RECOVERY_GUIDE.md

Do not write a single line of code until you have read all of these.

STEP 1.2 — Install react-helmet-async (if not already installed):
Check artifacts/marketplace/package.json — if react-helmet-async is not present, install it:
  cd artifacts/marketplace && pnpm add react-helmet-async
If already installed → skip, do not reinstall.

STEP 1.3 — Wrap app with HelmetProvider:
In artifacts/marketplace/src/main.tsx:
  Import HelmetProvider from react-helmet-async.
  Wrap the entire app tree with <HelmetProvider>.
  Do not change anything else in this file.

STEP 1.4 — Create SEO component:
Create file: artifacts/marketplace/src/components/SEO.tsx

Requirements:
  - Props: title, description, image (optional), url (optional), type (optional — default "website"), noindex (optional boolean)
  - title: always append " | SYANO — سوق سوريا" unless title already contains "SYANO"
  - Use react-helmet-async Helmet component
  - Output these tags:
      <title>{fullTitle}</title>
      <meta name="description" content={description} />
      <meta name="robots" content={noindex ? "noindex,nofollow" : "index,follow"} />
      <link rel="canonical" href={url ?? window.location.href} />
      <meta property="og:title" content={fullTitle} />
      <meta property="og:description" content={description} />
      <meta property="og:image" content={image ?? "/og-default.jpg"} />
      <meta property="og:url" content={url ?? window.location.href} />
      <meta property="og:type" content={type} />
      <meta property="og:site_name" content="SYANO — سوق سوريا" />
      <meta name="twitter:card" content="summary_large_image" />
      <meta name="twitter:title" content={fullTitle} />
      <meta name="twitter:description" content={description} />
      <meta name="twitter:image" content={image ?? "/og-default.jpg"} />
  - No `any` TypeScript type
  - Export as default

STEP 1.5 — Create JSON-LD component:
Create file: artifacts/marketplace/src/components/JsonLd.tsx

Requirements:
  - Props: data (Record<string, unknown>)
  - Renders: <script type="application/ld+json">{JSON.stringify(data)}</script> inside Helmet
  - No `any` TypeScript type
  - Export as default

STEP 1.6 — Add SEO to all major pages:
For each page listed below, import SEO and JsonLd, add <SEO> at the top of the JSX return, and add appropriate JSON-LD structured data.
Do not redesign any page. Do not change any existing JSX except adding the SEO and JsonLd components at the top of the return statement.
Do not hardcode any visible text — all strings must come from i18n or existing constants.

Pages to update:
  1. Home page (الرئيسية):
     - SEO: title from i18n key seo.home.title, description from seo.home.description
     - JSON-LD: WebSite schema with name "SYANO — سوق سوريا", url "https://syanomarket.online", potentialAction SearchAction

  2. Shop page (تسوق):
     - SEO: title from i18n key seo.shop.title, description from seo.shop.description
     - JSON-LD: ItemList schema (empty shell — products added dynamically when loaded)

  3. Product detail page:
     - SEO: title = product.name_ar (in Arabic) or product.name (in English) based on active locale
     - description = product.description (truncated to 155 chars)
     - image = product.images[0] if available
     - url = https://syanomarket.online/products/{product.id}
     - JSON-LD: Product schema with name, description, image, offers (price, currency, availability)

  4. Store page (المتاجر):
     - SEO: title = store.name + " | SYANO"
     - description from store.description if available
     - JSON-LD: Store/LocalBusiness schema with name, url

  5. Search results page:
     - SEO: title = search query + " | SYANO", noindex: true (search result pages must not be indexed)
     - No JSON-LD needed

  6. Deals page (العروض):
     - SEO: title from i18n key seo.deals.title, description from seo.deals.description
     - JSON-LD: SpecialAnnouncement or ItemList schema

  7. Login / Register pages:
     - SEO: noindex: true
     - No JSON-LD needed

STEP 1.7 — Add i18n keys for SEO:
Add to artifacts/marketplace/src/i18n/ar.json under "seo" namespace:
  "seo": {
    "home": {
      "title": "سوق سوريا الإلكتروني — تسوق أفضل المنتجات",
      "description": "اكتشف آلاف المنتجات من متاجر سورية موثوقة. إلكترونيات، ملابس، مستلزمات منزلية والمزيد."
    },
    "shop": {
      "title": "تسوق — جميع المنتجات",
      "description": "تصفح آلاف المنتجات من مختلف الفئات على سوق سوريا."
    },
    "deals": {
      "title": "العروض والخصومات",
      "description": "أفضل العروض والخصومات اليومية على سوق سوريا."
    }
  }

Add to artifacts/marketplace/src/i18n/en.json under "seo" namespace:
  "seo": {
    "home": {
      "title": "SYANO — Syria's Online Marketplace",
      "description": "Discover thousands of products from trusted Syrian stores. Electronics, clothing, home goods and more."
    },
    "shop": {
      "title": "Shop — All Products",
      "description": "Browse thousands of products across all categories on SYANO."
    },
    "deals": {
      "title": "Deals & Discounts",
      "description": "Best daily deals and discounts on SYANO marketplace."
    }
  }

STEP 1.8 — Add default OG image:
Create or confirm file exists: artifacts/marketplace/public/og-default.jpg
If it does not exist → create a simple placeholder using a canvas script or copy any existing image and rename it to og-default.jpg.
Dimensions must be 1200×630px. If no image generation is possible → create a 1×1 white pixel JPEG as placeholder and note it must be replaced before launch.

STEP 1.9 — Add sitemap endpoint to API server:
In artifacts/api-server/src/routes/ — create new file: sitemap.ts

Requirements:
  - GET /sitemap.xml
  - Fetches all active products (id, updated_at) and all active seller stores (id, updated_at) from the database using existing Drizzle queries
  - Returns valid XML sitemap with:
      - Static URLs: /, /shop, /deals, /stores (changefreq: daily, priority: 1.0)
      - Product URLs: /products/{id} (changefreq: weekly, priority: 0.8)
      - Store URLs: /stores/{id} (changefreq: weekly, priority: 0.7)
  - Sets Content-Type: application/xml
  - No `any` TypeScript type
  - Must use existing db instance — do not create a new connection
  - Register this route in the main router file (read the router file first to find the correct registration pattern)

STEP 1.10 — Add robots.txt:
Create file: artifacts/marketplace/public/robots.txt
Content:
  User-agent: *
  Allow: /
  Disallow: /admin/
  Disallow: /seller/
  Disallow: /courier/
  Disallow: /login
  Disallow: /register
  Disallow: /search?
  Sitemap: https://syanomarket.online/sitemap.xml

═══════════════════════════════════════
PART 2 — VERIFICATION
═══════════════════════════════════════

STEP 2.1 — TypeScript check — zero errors required:
Run: cd artifacts/api-server && npx tsc --noEmit
Run: cd artifacts/marketplace && npx tsc --noEmit
If any errors exist → fix every single one before continuing.
Show full output of both commands.

STEP 2.2 — Test sitemap endpoint:
GET http://localhost:8080/sitemap.xml
Must return HTTP 200 with Content-Type: application/xml
Show first 30 lines of the XML response.

STEP 2.3 — Test robots.txt:
GET http://localhost:5000/robots.txt
Must return HTTP 200
Show full content.

STEP 2.4 — Confirm SEO component renders correctly:
Start the marketplace dev server if not running.
Check browser source of the home page — confirm <title>, <meta name="description">, and <meta property="og:title"> are present in the HTML.

═══════════════════════════════════════
PART 3 — UPDATE RECOVERY FILES
═══════════════════════════════════════

STEP 3.1 — Overwrite CURRENT_STATE.md with exactly this content:

# SYANO — Current State
Last updated: June 16, 2026

## Project Identity
- Name: SYANO — سوق سوريا
- Type: Multi-vendor marketplace
- Domain: syanomarket.online
- GitHub: https://github.com/syano0007/syano07007
- Status: Pre-launch — Phase 11 in progress

## Active Phase
Phase 11 — Launch Preparation
Completed: Prompts 1, 2, 3, 4, 5, 6, 7, 8
Remaining: Prompt 9 (Accessibility), Prompt 10 (Performance Baseline)

## Last Session Work — June 16, 2026
### Phase 11 — Prompt 8 — SEO Layer ✅

1. react-helmet-async installed in artifacts/marketplace
2. HelmetProvider added to artifacts/marketplace/src/main.tsx
3. SEO component created: artifacts/marketplace/src/components/SEO.tsx
   - Props: title, description, image, url, type, noindex
   - Outputs: title, meta description, robots, canonical, Open Graph, Twitter Card
4. JsonLd component created: artifacts/marketplace/src/components/JsonLd.tsx
   - Renders JSON-LD structured data via Helmet script tag
5. SEO added to pages: Home, Shop, Product detail, Store, Search results, Deals, Login, Register
6. JSON-LD schemas added: WebSite (home), Product (product detail), LocalBusiness (store), ItemList (shop/deals)
7. i18n keys added under "seo" namespace in ar.json and en.json
8. Default OG image: artifacts/marketplace/public/og-default.jpg
9. Sitemap endpoint: GET /sitemap.xml — returns all products + stores + static URLs
   File: artifacts/api-server/src/routes/sitemap.ts
10. robots.txt: artifacts/marketplace/public/robots.txt

### Phase 11 — Prompt 7 — Email OTP ✅ (previous session)
1. Resend email service: artifacts/api-server/src/services/emailService.ts
2. Domain syanomarket.online verified on Resend: DKIM ✅ SPF ✅ MX ✅
3. OTP flow: POST /api/auth/register → email → POST /api/auth/verify-email → JWT
4. Bug fixed: verification.ts used FROM_EMAIL (wrong) → corrected to EMAIL_FROM with correct domain

## Required Replit Secrets — ALL must be set:
   SESSION_SECRET
   RESEND_API_KEY
   EMAIL_FROM              = noreply@syanomarket.online
   CORS_ORIGIN             = https://syanomarket.online
   SITE_URL                = https://syanomarket.online
   ROOT_ADMIN_PASSWORD
   VAPID_EMAIL             = mailto:admin@syanomarket.online
   VAPID_PRIVATE_KEY
   VAPID_PUBLIC_KEY
   VITE_SUPPORT_PHONE
   EMBEDDING_SERVICE_URL   = http://localhost:8001
   ENABLE_EMAIL_VERIFICATION = true

## Embedding Service
- Status: TF-IDF fallback — INTENTIONAL — do not install sentence-transformers
- Full model installed only once before final production launch

## Database State
- 33 tables, fully migrated
- 42 products with vector embeddings
- pgvector enabled — vector(384) on products table

## Test Accounts
- Admin:   delewatiamer7@gmail.com
- Seller:  delewatiamer8@gmail.com
- Courier: delewatiamer9@gmail.com

STEP 3.2 — Update PROJECT_STATUS.md:
Prepend exactly this block at the very top:

# SYANO — Project Status
Last updated: June 16, 2026

## Phase 11 — Launch Preparation
- Prompt 1  — Launch Readiness Audit     ✅ Complete
- Prompt 2  — Critical & High Fixes      ✅ Complete
- Prompt 3  — Error Handling Layer       ✅ Complete
- Prompt 4  — Data Quality               ✅ Complete
- Prompt 5  — Mobile Readiness           ✅ Complete
- Prompt 6  — Security Review            ✅ Complete
- Prompt 7  — Email OTP Verification     ✅ Complete
- Prompt 8  — SEO Layer                  ✅ Complete
- Prompt 9  — Accessibility              ⏳ Next
- Prompt 10 — Performance Baseline       ⏳ Not started

---

STEP 3.3 — Update RECOVERY_GUIDE.md:
In the RECOVERY_GUIDE.md file, find the section "## EMBEDDING SERVICE" and confirm it still says:
  "Do NOT install sentence-transformers or torch during any development session"
If that line is missing → add it back exactly as written.
Do not change anything else in RECOVERY_GUIDE.md.

STEP 3.4 — Update replit.md (or README.md — whichever exists at root):
Prepend exactly this block at the very top:

# SYANO — syanomarket.online
Last verified working: June 16, 2026
Workflows: API Server ✅ | Marketplace ✅ | Embedding Service ✅ (TF-IDF mode)
Secrets: All 12 loaded ✅
SEO Layer: react-helmet-async ✅ | sitemap.xml ✅ | robots.txt ✅ | JSON-LD ✅
Next task: Phase 11 — Prompt 9 — Accessibility

---

═══════════════════════════════════════
PART 4 — FINAL SUMMARY
═══════════════════════════════════════

STEP 4.1 — Print final summary table:

| Check | Expected | Actual | Status |
|---|---|---|---|
| react-helmet-async installed | ✅ | ? | ✅/❌ |
| HelmetProvider in main.tsx | ✅ | ? | ✅/❌ |
| SEO component created | ✅ | ? | ✅/❌ |
| JsonLd component created | ✅ | ? | ✅/❌ |
| SEO added to Home page | ✅ | ? | ✅/❌ |
| SEO added to Shop page | ✅ | ? | ✅/❌ |
| SEO added to Product detail | ✅ | ? | ✅/❌ |
| SEO added to Store page | ✅ | ? | ✅/❌ |
| SEO added to Search results | noindex:true | ? | ✅/❌ |
| SEO added to Deals page | ✅ | ? | ✅/❌ |
| SEO added to Login/Register | noindex:true | ? | ✅/❌ |
| i18n keys added (ar.json) | ✅ | ? | ✅/❌ |
| i18n keys added (en.json) | ✅ | ? | ✅/❌ |
| og-default.jpg exists | ✅ | ? | ✅/❌ |
| robots.txt exists | ✅ | ? | ✅/❌ |
| /sitemap.xml returns 200 | ✅ | ? | ✅/❌ |
| /robots.txt returns 200 | ✅ | ? | ✅/❌ |
| API Server TypeScript | 0 errors | ? | ✅/❌ |
| Marketplace TypeScript | 0 errors | ? | ✅/❌ |
| CURRENT_STATE.md updated | ✅ | ? | ✅/❌ |
| PROJECT_STATUS.md updated | ✅ | ? | ✅/❌ |
| RECOVERY_GUIDE.md verified | ✅ | ? | ✅/❌ |
| replit.md updated | ✅ | ? | ✅/❌ |

If any row shows ❌ → fix it immediately. Do not finish until every row is ✅.