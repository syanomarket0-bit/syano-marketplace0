The `brainstorming` skill writes approved design specs to this folder before any implementation begins — it is the hard-gate output that must exist and be approved before code is written.

**Naming convention:** `YYYY-MM-DD-<short-topic>-design.md` (e.g. `2026-06-27-vendor-dashboard-design.md`) — specified by the brainstorming skill's own documentation.
